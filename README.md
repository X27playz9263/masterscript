place masterscript in your home directory
then type run:
chmod +x masterscript
then run ./masterscript
this wiil create a scripts folder automatically and add 6 core feature scripts, it will create them again if removed or placed elsewhere

Step 1:
chmod +x masterscript
this makes it executable, so now it can actually run
Step 2:
ln -s /path/to/masterscript /usr/local/bin
this creates a symlink, so you can call it just by typing masterscript in the terminal
Step 3:
enjoy it
